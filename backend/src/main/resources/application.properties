# Elide JSON API Configuration
elide.json-api.enabled=true
elide.json-api.path=/api

# Disable other Elide features if not needed
elide.graphql.enabled=false
elide.async.enabled=false

# Enable Spring Data REST (sometimes needed for Elide)
spring.data.rest.base-path=/